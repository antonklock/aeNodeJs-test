'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isObjectOf;

var _is = require('./is');

var _is2 = _interopRequireDefault(_is);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function isObjectOf(object) {
  for (var _len = arguments.length, types = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    types[_key - 1] = arguments[_key];
  }

  if (types.length === 0) throw new Error('is.objectOf requires at least one type.');

  if ((0, _is2.default)(object, Array)) return false;

  var atLeastOneKey = false;

  for (var key in object) {
    if (!_is2.default.apply(undefined, [object[key]].concat(types))) return false;else atLeastOneKey = true;
  }return atLeastOneKey;
}